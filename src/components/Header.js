import React from "react"

const Header = () => {
    return (
        <header>
            <h1 className="main-heading">Retrieve Weather Forecast</h1>
        </header>
    )
}

export default Header